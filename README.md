# 북한산장 원가 계산기

재료 구매가 → 반제품 1g당 원가 → 메뉴 원가로 이어지는 계산기.
`site/index.html` 한 파일로 동작하며 별도 빌드가 없습니다.

- 입력값은 보는 사람의 브라우저에만 저장됩니다(localStorage).
- 기본값은 HTML 안에 들어 있어 어느 기기에서 열어도 동일합니다.
- 검색엔진 색인은 `noindex` 메타태그와 robots.txt로 차단했습니다.
